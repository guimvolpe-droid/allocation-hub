# bkp/ — reutilizáveis fora de uso (gitignored; só este índice é versionado)

Regra: cada lote em `bkp/AAAA-MM-<slug>/`, indexado aqui. Nada em bkp/ é lido pelo build.

## 2026-07-html-gerado

HTML gerado para apresentação (fora de uso, reutilizável se voltar a demo Lyncas):

- `arquitetura-visual.html` — visão visual da arquitetura (Clean Architecture) gerada para apoio.
- `cockpit-lyncas.html` — cockpit/painel de apresentação gerado para o contexto Lyncas.
